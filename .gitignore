# dependencies
node_modules

# build output
dist
dist-ssr

# local env and secrets (never commit keys; API keys live only in Secret Manager)
.env
.env.local
.env.*.local
secrets.md
temp_key*
# temporary key handoff files (real keys live only in Secret Manager, never in the repo)
plaid.md
tmp_plaid.md

# firebase
.firebase
firebase-debug.log
firestore-debug.log
ui-debug.log
functions/lib

# firebase emulator state (regenerate with npm run seed:emulator)
emulator-data

# local secrets for the functions emulator (never committed)
functions/.secret.local

# editor and OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Local seed data (personal financials, do not commit)
scripts/tx-parsed.json

# Generated codebase bundle (snapshots every source file, never commit)
codebase-review.md

# typescript incremental build state
tsconfig.tsbuildinfo
